class Solution:
    def subsets(self, nums: List[int]) -> List[List[int]]:
        """
        部分集合を返す問題。
        空集合も含める。
        ダブりはNG。
        順番は自由。
        ずるいが、これはDFS（Depth First Search, Breadth First Searchの略）。
        再帰的に求める。
        含む、含まないの2分岐のツリーを作成するイメージ。
        """
        if len(nums) == 0:
            return []
        def yesno(self, subset: List[int]):

        for i in range(len(nums)):
            yesno()
