class Solution:
    def networkDelayTime(self, times: List[List[int]], n: int, k: int) -> int:
        """
        1-indexed
        指定のkのnodeからシグナルを発したときに、すべてのnodeにシグナルが到達する最短時間は
        dp[]
        各ノードについて記録する。
        各ノード
        """
