class KthLargest:

    def __init__(self, k: int, nums: List[int]):
        new KthLargest(k, nums)

    def add(self, val: int) -> int:
        """
        ここでやることは？
        __init__はどう呼び出されるの？
        リストが完了するまで繰り返し処理だと思うが？
        何を要求されているんだ？
        """



# Your KthLargest object will be instantiated and called as such:
# obj = KthLargest(k, nums)
# param_1 = obj.add(val)
